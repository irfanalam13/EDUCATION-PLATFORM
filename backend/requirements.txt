# Direct dependencies, pinned for reproducible builds.
# Versions resolved against Python 3.13 + Django 6.0 (see CI). Regenerate with:
#   pip install -U <pkg> && pip freeze   (then update the pin here)

# Core
Django==6.1.1
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
django-environ==0.14.0
django-cors-headers==4.9.0
django-filter==25.2
drf-spectacular==0.30.0
django-extensions==4.1

# Server
gunicorn==26.2.0
whitenoise==6.12.0          # serve hashed static files from the app server

# Database
psycopg[binary]==3.3.5

# Async / cache
celery==5.6.3
redis==8.1.0
django-redis==7.0.0

# Storage / media
django-storages[s3]==1.14.6
boto3==1.43.89
Pillow==12.3.0

# AI / RAG
google-genai==1.51.0        # Gemini chat (primary answer provider)
anthropic==0.111.0          # Claude chat (RAG + general answers; fallback)
pymupdf==1.28.2           # PDF text extraction
# Embeddings are pluggable (AI_EMBEDDING_PROVIDER). Install the one you use:
voyageai==0.5.0             # optional: Voyage embeddings (Anthropic-recommended)
openai==2.43.0              # optional: OpenAI embeddings

# Auth
google-auth==2.57.1         # Google OAuth ID-token verification

# Payments (apps.billing) — gateway REST calls (Stripe / Khalti / eSewa)
httpx==0.28.1

# Reporting (institution analytics PDF + Excel exports)
reportlab==4.4.5
openpyxl==3.1.5

# Observability (optional; activated when SENTRY_DSN is set)
sentry-sdk==2.68.1
